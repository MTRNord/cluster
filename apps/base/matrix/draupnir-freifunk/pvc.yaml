apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: pantalaimon-freifunk-v2
    namespace: matrix
spec:
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 2Gi
