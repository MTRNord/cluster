apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: matrix
resources:
    - deployment.yaml
    - service.yaml
configMapGenerator:
    - name: connectivity-tester-config
      namespace: matrix
      files:
        - config.json=config.json
