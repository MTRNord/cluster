---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: draupnir-testing-1700824-keydb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: draupnir-testing-1700824-keydb
  template:
    metadata:
      labels:
        app: draupnir-testing-1700824-keydb
    spec:
      containers:
        - name: draupnir-testing-1700824-keydb
          image: eqalpha/keydb:latest
          imagePullPolicy: Always
          command: ["keydb-server"]
          args: [ "/etc/keydb/keydb.conf", "--protected-mode", "no"]
          ports:
            - containerPort: 6379
      tolerations:
        - key: arch
          operator: Equal
          value: arm64
          effect: NoSchedule
      livenessProbe:
        exec:
          command:
          - sh
          - -c
          - keydb-cli ping
        initialDelaySeconds: 10
        timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: draupnir-testing-1700824-keydb
spec:
  selector:
    app: draupnir-testing-1700824-keydb
  ports:
  - name: redis
    protocol: TCP
    port: 6379
    targetPort: 6379
