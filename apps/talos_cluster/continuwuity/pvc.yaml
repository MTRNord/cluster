apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: continuwuity
spec:
    resources:
        requests:
            storage: 10Gi
    storageClassName: longhorn
    volumeMode: Filesystem
    accessModes:
        - ReadWriteOnce
