apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: continuwuity
spec:
    resources:
        requests:
            storage: 5Gi
    storageClassName: ceph-filesystem
    volumeMode: Filesystem
    accessModes:
        - ReadWriteOnce
