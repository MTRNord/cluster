apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml
  - secret.yaml
  - ingress.yaml
  - routing.yaml

patches:
  - path: servicemonitor-patch.yaml
    target:
      group: monitoring.coreos.com
      version: v1
      kind: ServiceMonitor
      name: stalwart-mail
