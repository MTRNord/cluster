apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: jenkins
  namespace: jenkins

resources:
  - namespace.yaml
  - helm-repository.yaml
  - operator-release.yaml
  - instance
