apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: convex-data
  namespace: convex
spec:
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
