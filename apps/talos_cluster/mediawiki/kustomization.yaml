apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mediawiki
resources:
  - namespace.yaml
  - secret.yaml
  - configmap.yaml
  - pvc.yaml
  - mariadb.yaml
  - deployment.yaml
  # route.yaml is intentionally omitted until after initial setup
  # Add it back once the wiki is installed and verified via port-forward
