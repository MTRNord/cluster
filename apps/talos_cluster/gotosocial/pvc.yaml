apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: gotosocial
    namespace: gotosocial
spec:
    resources:
        requests:
            storage: 1Gi
    storageClassName: longhorn
    volumeMode: Filesystem
    accessModes:
        - ReadWriteMany
