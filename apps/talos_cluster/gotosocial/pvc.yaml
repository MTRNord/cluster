apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gotosocial
  namespace: gotosocial
spec:
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
