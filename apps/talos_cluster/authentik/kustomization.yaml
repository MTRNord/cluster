apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: authentik
resources:
    - repository.yaml
    - ingress.yaml
    - certificates.yaml
    # TODO: Enable once db is migrated
    #- release.yaml
